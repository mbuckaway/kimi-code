Lists the models configured for this session. Each entry shows the alias,
provider, wire model id, display name, and context window, and marks which
aliases are the current model, the configured default model, and the configured
planning model. An optional `provider` filter narrows the list to one provider.
