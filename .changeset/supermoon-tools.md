---
"@moonshot-ai/kimi-code": minor
---

Replace the /supermoon slash command with the EnterSupermoonMode and ExitSupermoonMode tools, so the agent can turn Supermoon mode on and off itself. The agent calls EnterSupermoonMode to enable the mode; ExitSupermoonMode turns it off.
