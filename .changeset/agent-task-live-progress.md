---
"@moonshot-ai/kimi-code": patch
---

Show live progress for subagents and background tasks: the Agent tool card lists each sub-tool the subagent runs, background tasks show a running spinner, elapsed time, and current activity, the /tasks list shows live elapsed time, and the WaitFor card animates while waiting.
