---
"@moonshot-ai/kimi-code": patch
---

The plan-review gate now also applies to sessions entered via /plan, Shift-Tab, the --plan flag, default_plan_mode, or resume: the plan-mode reminders instruct running the plan-review skill once before ExitPlanMode.
