---
"@moonshot-ai/kimi-code": minor
---

Start new sessions in Swarm mode by default, and let `AgentSwarm` take a list of distinct prompts so differently-shaped subtasks can run as one parallel swarm.
